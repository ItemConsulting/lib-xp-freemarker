---
"@item-enonic-types/lib-freemarker": minor
---

Template edits are now picked up immediately when XP runs in development mode, instead of being served from
FreeMarker's cache until its update delay elapses.
